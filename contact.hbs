<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Contact Us</title>
        <link rel="stylesheet" href="/home.css">
        <link rel="stylesheet" href="/index.css">
        <script
            src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
        <script
            src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
        <script src="/js/script.js"></script> <!-- Your main JS file -->
    </head>
    <body>
        <nav class="navbar">
        <div class="logo">TravelTracker</div>
        <div class="menu-toggle" id="menuToggle">&#9776;</div>
        <div class="nav-container" id="navContainer">
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/contact">Contact_Us</a></li>
                <li><a href="/login">Login</a></li>
                <li><a href="/signup">Sign Up</a></li>
            </ul>
        </div>
    </nav>
        <main class="contact-hero-section">
            <section class="contact-header">
                <h1>Contact <span>Us</span></h1>
                <p>We’d love to hear from you! Fill in the form below and we’ll
                    get back to you soon.</p>
            </section>

            <div class="contact-container">
                <form class="contact-form" id="contact_us-form">
                    <div class="input-group">
                        <label for="contact-name">Your Name</label>
                        <input type="text" name="from_name" id="contact-name"
                            class="contact-input-name"
                            placeholder="Enter your name" required>
                    </div>

                    <div class="input-group">
                        <label for="contact-email">Your Email</label>
                        <input type="email" name="from_email" id="contact-email"
                            class="contact-input-email"
                            placeholder="Enter your email" required>
                    </div>

                    <div class="input-group">
                        <label for="contact-subject">Subject</label>
                        <input type="text" name="subject" id="contact-subject"
                            class="contact-input-subject"
                            placeholder="Enter subject" required>
                    </div>

                    <div class="input-group">
                        <label for="contact-message">Your Message</label>
                        <textarea id="contact-message" name="message"
                            class="contact-input-message"
                            placeholder="Enter your message"
                            required></textarea>
                    </div>

                    <button type="submit" class="contact-submit-btn"
                        value="submit">Send Message</button>
                </form>

            </div>
        </main>
        <script>
        // contact form email js
        document.addEventListener("DOMContentLoaded", function () {
    emailjs.init("WNxuWcwkLox2TnUN8"); // Replace with your actual EmailJS User ID

    document.getElementById("contact_us-form").addEventListener("submit", function (event) {
        event.preventDefault(); // Prevent form from reloading

        // Get form input values
        let name = document.querySelector(".contact-input-name").value;
        let email = document.querySelector(".contact-input-email").value;
        let subject = document.querySelector(".contact-input-subject").value;
        let message = document.querySelector(".contact-input-message").value;
        let submitBtn = document.querySelector(".contact-submit-btn");

        // Change button text to "Sending..."
        submitBtn.textContent = "Sending...";
        submitBtn.disabled = true; // Disable button to prevent multiple clicks

        // Send Email using EmailJS
        emailjs.send("service_wt8iyfx", "template_5s36efn", {
            from_name: name,  
            from_email: email, 
            subject: subject,  
            message: message   
        }).then(
            function (response) {
                submitBtn.textContent = "Send Message"; // Restore button text
                submitBtn.disabled = false; // Re-enable button
                alert("Email sent successfully!");
                document.getElementById("contact_us-form").reset(); // Clear form after successful send
            },
            function (error) {
                submitBtn.textContent = "Send Message"; // Restore button text
                submitBtn.disabled = false; // Re-enable button
                alert("Failed to send email. Please try again.");
                console.error("EmailJS Error:", error);
            }
        );
    });
});

    </script>
        <script defer src="/script.js"></script>
        <script>
            const menuToggle = document.getElementById("menuToggle");
            const navContainer = document.getElementById("navContainer");

            menuToggle.addEventListener("click", function() {
                navContainer.classList.toggle("active");
                menuToggle.classList.toggle("open");
            });
        </script>
    </body>
</html>
